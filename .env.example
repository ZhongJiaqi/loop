# VITE_FIREBASE_VAPID_KEY: Web Push 公钥（Firebase Console → Cloud Messaging → Web Push certificates）。
# 仅「推送提醒」功能需要；不配置时应用其余功能不受影响。
# 对应的 VAPID 私钥不放这里——它以 Firebase Functions secret（VAPID_PRIVATE_KEY）形式配置。
VITE_FIREBASE_VAPID_KEY="YOUR_VAPID_PUBLIC_KEY"

# Firebase 客户端配置不走环境变量：见 firebase-applet-config.json（Web 端配置本就是公开的，
# 数据安全由 firestore.rules 的 owner-only 校验守护）。自部署请替换为你自己的 Firebase 项目配置。
